#include <stdio.h>

#define macro 
// this project is for making ccwc command line 
//My own wc tool 

/// @brief a function that counts bytes
/// @param  bytes : bytes counted in the text file 
/// @param command_str : 
/// @return number of bytes in a file

int countBytes(bytes, command_str)
{
    /*char filename[] = "test.txt";
    for (size_t i = 0; i < count; i++)
    {
        /* code */
    }
    
    /*scanf("%s", &bytes);
    printf(" %d", bytes);
    return 0;
    */


}